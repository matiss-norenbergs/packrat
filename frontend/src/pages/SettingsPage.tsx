import { PlaceholderPage } from "./PlaceholderPage"

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Not implemented in this working skeleton — download directory and concurrency are configured via environment variables for now."
    />
  )
}
