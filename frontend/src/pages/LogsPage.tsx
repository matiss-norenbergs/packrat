import { PlaceholderPage } from "./PlaceholderPage"

export function LogsPage() {
  return (
    <PlaceholderPage
      title="Logs"
      description="Not implemented in this working skeleton — yt-dlp stdout/stderr is captured per download and stored in the database, but there is no viewer UI yet."
    />
  )
}
