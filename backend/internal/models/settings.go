package models

// Settings keys stored in the key/value settings table. Only these two are
// used by the working skeleton; the rest of the spec's Settings sections are
// not wired up yet.
const (
	SettingDownloadDirectory      = "download_directory"
	SettingMaxConcurrentDownloads = "max_concurrent_downloads"
)
